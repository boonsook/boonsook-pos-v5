# TEMPORARY — preview deploy build 559 (Phase 560) for owner smoke (remove before merge)
name: Preview Deploy (560)
on:
  push:
    branches: ['claude/phase-560-service-transfer-bank-route']
jobs:
  preview:
    runs-on: ubuntu-latest
    name: Deploy preview 560
    steps:
      - uses: actions/checkout@v5
      - uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy . --project-name=boonsook-pos-v5 --branch=claude-phase-560 --commit-dirty=true --commit-message="Preview 560 ${{ github.sha }}"
