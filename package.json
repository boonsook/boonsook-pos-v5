{
  "name": "boonsook-pos-v5",
  "version": "5.69.18",
  "private": true,
  "description": "Boonsook POS PRO V5 — Thai POS PWA",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "eslint . --max-warnings=99999",
    "lint:strict": "eslint .",
    "lint:errors": "eslint . --quiet",
    "lint:fix": "eslint . --fix",
    "test:e2e": "playwright test",
    "verify": "npm run lint && npm test && npm run test:e2e",
    "verify:security": "node scripts/leave_spoof_test.js",
    "verify:accounting": "node scripts/accounting_integrity_smoke.js",
    "verify:je": "node scripts/verify_je_fix.js",
    "backfill:orphans": "node scripts/backfill_orphan_journals.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.60.0",
    "eslint": "^10.4.0"
  }
}
